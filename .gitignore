CMakeCache.txt
__pycache__/
.vscode/
build/
install/
log/
config/calibration.yaml

# Claude Code session/agent state
.claude/

# Local stereo-calibration debug artefacts — regenerated by the workflow,
# not portable across machines. Keep stereo_calib.yaml in tree (that's the
# tracked calibration), but skip the optimizer's backup variants and the
# rectified-pair previews used only for visual sanity-checks.
config/stereo_calib.yaml.tz*
scripts/stereo_calib_rectified_sample.png
testing/rectified_samples/
